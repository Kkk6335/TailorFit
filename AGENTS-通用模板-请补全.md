# AGENTS.md

> 本文件用于指导 AI（Codex、Claude Code、GPT、Gemini、Grok 等）参与本项目开发。
>
> 它是**当前编码规则**、**项目约束索引**和**交接模板**，不是开发日志或历史故障档案。
>
> 目标：让下一位 AI 在不猜测的前提下，快速理解当前项目并做最小正确改动。

## 0. 使用方式（先读）

开始任务前，按此顺序阅读：

1. `项目概览`、`命令与验证`、`仓库地图`。
2. `当前限制`、`Known Pitfalls`、`Design Decisions`。
3. 与任务相关的源码、测试和模块文档；禁止凭印象编造。

任务结束后：

1. 完成“AI 完成任务后的强制检查”。
2. 只同步受影响的文档。
3. 只有变更具有长期回溯价值时，才在 `docs/ai-log/` 写简短日志。
4. 按“AI 交接”模板说明结果与验证边界。

### 优先级

发生冲突时：

1. 用户本次明确指令。
2. 更靠近被修改文件的 `AGENTS.md`。
3. 本文件。
4. 通用最佳实践。

## 一、项目概览（项目填写，保持简短）

### 项目简介

- 项目是什么：`TODO(project)`。
- 解决什么问题：`TODO(project)`。
- 当前阶段：`TODO(project)`（MVP / Beta / Production）。
- 当前开发重点：`TODO(project)`。

### 技术栈

| 层 | 技术 |
| --- | --- |
| Frontend | `TODO(project)` |
| Backend | `TODO(project)` |
| Database | `TODO(project)` |
| Deployment | `TODO(project)` |
| AI | `TODO(project)` |
| Storage | `TODO(project)` |

### 仓库地图

只写真实存在、会影响改动位置的目录：

| 路径 | 用途 | 备注 |
| --- | --- | --- |
| `TODO(project)` | 源码 | |
| `TODO(project)` | 测试 | |
| `TODO(project)` | 脚本/工具 | |
| `TODO(project)` | 构建产物 | 默认不要手改 |

### 命令与验证（必填）

| 目的 | 命令 |
| --- | --- |
| 安装依赖 | `TODO(project)` |
| 本地开发 | `TODO(project)` |
| 测试 | `TODO(project)` |
| Lint / 类型检查 | `TODO(project)` |
| 构建 | `TODO(project)` |

完成前至少运行与改动相关的验证。未配置自动测试时，至少执行可用的语法、类型、构建或关键路径自检，并说明验证边界。验证失败、无法运行或仅完成部分验证时，不得描述为“已验证通过”；必须明确说明失败项和未验证范围。单元测试不等于浏览器、设备、无障碍、真实 API 或生产环境验证。

## 二、开发硬规则（通用，少改）

### 修改原则

始终：

- 先理解现有实现，再改。
- 优先复用已有模块与模式。
- 做完成任务所需的最小改动。
- 跟随现有目录结构与代码风格。
- 默认保持向后兼容，除非用户明确要求破坏性变更。

不要：

- 猜测业务逻辑、API、数据库结构或“应该存在”的功能。
- 为局部问题做全局重构、删除看不懂的代码或引入无必要依赖。
- 修改与当前任务无关的代码、文件或格式。
- 重复实现已有能力。

### 架构偏好

- 项目已有分层、模块边界或依赖方向时保持一致；除非任务明确要求，不主动重新分层或改变公共边界。
- UI 与核心业务逻辑尽量分离；新增文件优先放进已有目录。
- 新建目录、移动文件或改变公共边界需要明确理由，并同步受影响文档。

### 编码约束

- 命名准确表达用途；避免 `aaa`、`temp`、`doThing`、`test1`。
- 函数保持单一职责；优先小函数、早返回和低嵌套。
- 避免魔法数字、死代码、未使用变量和明显重复逻辑。
- 注释只解释非显而易见的业务、安全、性能原因。
- 网络、文件、数据库和 API 等外部边界必须有清晰失败路径；禁止空 `catch` 与静默吞错。

### 依赖、安全与性能

- 新增依赖前先确认现有代码和标准能力不能解决；工具级或小改动默认不新增依赖。
- 禁止硬编码、提交或输出 API Key、密码、Token、私钥；沿用项目既有密钥管理和输入校验。
- 避免明显的重复请求、重复计算、阻塞操作和无意义热路径渲染；需要优化时说明收益。

### Git

- 用户未明确要求时，不要 commit、push、建分支、开 PR 或修改 Git 配置。
- 用户要求提交时，暂存前检查文件范围与敏感信息；一次提交只包含一个逻辑变更。
- 提交说明优先使用 `feat:`、`fix:`、`docs:`、`refactor:`、`test:`、`style:` 或 `chore:`。

### 决策优先级

1. 正确性。
2. 稳定性与兼容性。
3. 最小修改。
4. 可维护性与可读性。
5. 简洁性、性能与开发效率。

## 三、文档同步与知识治理

### 文档同步

| 变更类型 | 需要同步 |
| --- | --- |
| 公共 API / 接口契约 | API 文档 |
| 安装、启动、开发流程 | README / 部署说明 |
| 环境变量 | `.env.example` 与相关说明 |
| 架构约定、限制、坑、设计决策 | 本文件对应节 |
| 用户可见功能增减 | README 与“当前已实现功能” |

若本次变更不影响文档，在交接中写“文档无需同步”；禁止为打卡而空改文档。

开发日志写在 `docs/ai-log/YYYY-MM-DD-<topic>.md`，只记录重大行为变更、不可直观看出的取舍、重要故障复盘或发布风险。纯格式调整、可从 Git diff 直接理解的改动无需新增日志。

### 防止 AGENTS 膨胀

- 只记录**当前有效、跨任务可复用、会改变实现决策**的知识；AGENTS 不承担历史归档职责。
- 能从 README、代码、测试或配置中直接明显推断出的事实，不重复写入项目知识。
- 同一根因只保留一条：后续同类发现应更新、合并或替换原条目，不追加近似记录。
- 已废弃、被替代、只适用于一次性实现、无法关联当前代码或测试的内容直接删除。
- 历史依据通过 Git 提交与 `docs/ai-log/` 查找；不要为了“可追溯”在 AGENTS 保留失效规则。
- `当前已实现功能` 最多 12 条，`当前限制` 最多 8 条，`Known Pitfalls` 最多 12 条，`Design Decisions` 最多 8 条。达到上限先合并、淘汰或拆到相应模块文档。
- 每条坑和决策必须给出当前路径、模块或验证命令；没有可验证落点就不写入。
- 每次涉及这些知识节时，先做一次去重和有效性检查；不能累积新增近似条目。

### AI 完成任务后的强制检查

每项结论只能是“已更新”或“无需更新（原因一句话）”：

- README
- AGENTS
- Known Pitfalls
- 已实现功能
- 当前限制
- Development Log

需要更新却未更新时任务未完成。

## 四、项目知识（持续维护，保持短）

### 当前已实现功能

只写会影响后续开发判断的重要已实现能力；普通功能列表放 README，最多 12 条。

- `TODO(project)` 例如：用户登录。

### 当前限制

只写当前不能做或故意不做的事情；最多 8 条。

- `TODO(project)` 例如：暂不支持多实例。

### Known Pitfalls

只记录当前高影响、非显而易见且会导致错误实现的坑；最多 12 条。删除失效条目，合并同根因条目。

```md
#### [类别] 标题
- 触发条件：什么场景会出错。
- 必须做法：当前实现必须遵守什么。
- 路径/测试：`path/to/file`、`验证命令`。
```

类别示例：数据库、部署、权限、缓存、接口、环境变量、第三方 API、AI、兼容性。

### Design Decisions

只记录仍有效、会约束后续设计的决策；最多 8 条。决策被推翻时，更新为新决策并在 Git 或开发日志保留依据，不保留废弃文本。

```md
#### 决策标题
- 决策：做了什么 / 没做什么。
- 原因：为什么。
- 约束：后续改动需要遵守什么。
- 路径/测试：`path/to/file`、`验证命令`。
```

## 五、AI 交接（回复中使用）

```md
【完成内容】
【修改文件】
【验证情况】（运行命令 / 结果；未运行项及原因）
【强制检查】
- README：已更新 / 无需更新（原因）
- AGENTS：已更新 / 无需更新（原因）
- Known Pitfalls：已更新 / 无需更新（原因）
- 已实现功能：已更新 / 无需更新（原因）
- 当前限制：已更新 / 无需更新（原因）
- Development Log：已写入路径 / 无需更新（原因）
【文档同步】
【需要关注】
```

## 六、禁止事项

- 提交、打印或持久化密钥、Token、密码、私钥。
- 编造功能、API、数据库字段、配置项或不存在的项目文件。
- 为了保存历史而累积已废弃、重复或低频规则。
- 删除 `docs/ai-log/` 历史日志，除非用户明确要求。
- 在未授权时执行 Git 发布类操作、无关重构或空改文档。
- 不确定时不得猜测；优先通过源码、测试、配置或项目文档确认，仍无法确定且会影响正确实现时再向用户确认。

除非用户明确要求，否则保持现有可观察行为一致。
